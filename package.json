{
  "name": "github_action_project",
  "version": "1.0.0",
  "scripts": {
    "lint": "echo 'No linter configured'",
    "test": "echo 'No tests configured'",
    "build": "echo 'No build step'"
  }
}
